# Personal, per-project Claude notes (do not share)
CLAUDE.local.md

# Secrets — never commit
.env
.env.*
*.token
secrets/

# OS / editor
.DS_Store
.vscode/*
!.vscode/extensions.json

# Sprint state — local working memory, not committed
.sprint-state/

# WIP draft files — not ready to commit
*.draft.md
